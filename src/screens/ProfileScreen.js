import React from 'react';
import { SafeAreaView, View, Text } from 'react-native';

export const ProfileScreen = () => {
    return (
        <SafeAreaView>
            <Text>Profile</Text>
        </SafeAreaView>
    );
};

